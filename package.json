{
  "name": "finance-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Личный финансовый трекер — backend на Node + libSQL (Turso), фронт — React/Tailwind в одном HTML.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  }
}
